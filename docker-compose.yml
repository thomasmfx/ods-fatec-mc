services:
  swagger-ui:
      image: swaggerapi/swagger-ui
      container_name: ods_docs
      ports:
        - "8081:8080"
      environment:
        SWAGGER_JSON: /tmp/contract.yaml
      volumes:
        - ./docs/ods_api_contract.yaml:/tmp/contract.yaml:ro